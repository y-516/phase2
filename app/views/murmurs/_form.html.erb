<% if @murmur.errors.any? %>
      <h2><%= @murmur.errors.count %>件のエラーがあります。</h2>
      <ul>
      <% @murmur.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
  <% end %>

<%= form_with(model: @murmur,local:true,url: choose_new_or_edit ) do|form|%>
  <div>
    <%=form.label:つぶやき内容%>
  </div>
  <div>
    <%=form.text_field:content%>
  </div>
  
  <%=form.submit '投稿'%>
  <%end%>